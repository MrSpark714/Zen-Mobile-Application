<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="16dp"
    android:paddingTop="12dp"
    android:paddingEnd="16dp"
    android:paddingBottom="12dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/tv_history_status_badge"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_history_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textColor="@color/zen_text_primary"
            android:textSize="14sp"
            tools:text="Wednesday, Aug 26, 2026" />

        <TextView
            android:id="@+id/tv_history_day_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:fontFamily="sans-serif"
            android:textColor="@color/zen_text_secondary"
            android:textSize="12sp"
            tools:text="Regular Class Session" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_history_status_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@drawable/bg_status_present"
        android:fontFamily="sans-serif-bold"
        android:letterSpacing="0.06"
        android:paddingStart="12dp"
        android:paddingTop="5dp"
        android:paddingEnd="12dp"
        android:paddingBottom="5dp"
        android:text="PRESENT"
        android:textColor="@color/status_present"
        android:textSize="11sp"
        tools:text="PRESENT" />

</RelativeLayout>
